name: dart_mock_locations_provider
description: A simple command-line application.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.16.1 <3.0.0"

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  build_runner: ^2.1.11
  freezed: ^2.0.3+1
  json_serializable:
  lints: ^1.0.0
dependencies:
  freezed_annotation: ^2.0.3
  gpx: ^2.1.1
  latlong2: ^0.8.1
  web_socket_channel: ^2.2.0
